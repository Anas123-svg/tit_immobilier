type User = {
  id: number;
  name: string;
  email: string;
  Gender: string;
  userLogin: string;
  service: string;
  contact: string;
  pronouns: string;
  photo: string;
  permissions: string[];
  documents: string[];
};

export type { User };
